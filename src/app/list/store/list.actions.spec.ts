// import * as fromList from './list.actions';

describe('loadItem', () => {
  it('should return an action', () => {
    pending()
    // expect(fromList.loadItemsSuccess().type).toBe('[List] Load Items Success');
  });
});
